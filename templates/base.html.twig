<!DOCTYPE html>
<html lang="fr">
    <head>
        {% include "components/_metadatas.html.twig" %}
        <title>{% block title %}Menuiserie Hillairet{% endblock %}</title>
            <link rel="icon" type="image/png" href="{{ asset('build/images/favicon.png') }}" />
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}
        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body class="bg-gray-100">
        <header>
            {% include "commons/_contact.html.twig" %}
            {% include "commons/_header.html.twig" %}
            {{ render(controller('App\\Controller\\CategoryController::index')) }}
        </header>
        {% block body %}{% endblock %}
        <footer>
            {% include "commons/_footer.html.twig" %}
        </footer>
    </body>
</html>
