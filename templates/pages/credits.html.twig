{% extends 'base.html.twig' %}

{% block title %}
	{{ parent() }}
	- Crédits Photos
{% endblock %}

{% block body %}
	<div class="container mx-auto">
		<h1 class="text-4xl font-bold text-primary text-center my-16">Crédits Photos</h1>
		{% for credit in projectImages %}
			<p class="text-2xl font-bold text-center my-16">{{ credit.credits }}</p>
		{% endfor %}
		{% for credit in categories %}
			<p class="text-2xl font-bold text-center my-16">{{ credit.credits }}</p>
		{% endfor %}
	</div>
{% endblock %}
